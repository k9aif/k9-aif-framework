# SPDX-License-Identifier: LicenseRef-K9AIF-Proprietary
# Generated by K9-AIF Generator on {{ timestamp }}

app:
  name: "{{ app_name }}"
  folder: "{{ app_folder }}"

demo:
  default_intent: "{{ default_intent }}"
  query: "{{ query }}"

governance:
  allowed_domains:
    - "{{ default_intent }}"

inference:
  provider: mock

  ollama:
    base_url: "http://localhost:11434"
    model: "llama3.1"

squads:
  path: "config/squads.yaml"

agents:
  path: "config/agents.yaml"