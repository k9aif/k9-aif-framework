# SPDX-License-Identifier: Apache-2.0
# Auto-generated by K9-AIF Generator
# Generated on {{ timestamp }}

"""
Default Squad for {{ app_name }}

This squad groups the domain agents that participate in the default workflow.
Extend this class with real squad logic as your application evolves.
"""

from k9_core.k9_squad.base_squad import BaseSquad

from agents.retrieval_agent import {{ app_name }}RetrievalAgent
from agents.enrichment_agent import {{ app_name }}EnrichmentAgent
from agents.summarizer_agent import {{ app_name }}SummarizerAgent


class {{ app_name }}DefaultSquad(BaseSquad):
    """
    Default squad for {{ app_name }}.
    """

    def __init__(self):
        super().__init__(name="{{ squad_name }}")

        self.retrieval_agent = {{ app_name }}RetrievalAgent()
        self.enrichment_agent = {{ app_name }}EnrichmentAgent()
        self.summarizer_agent = {{ app_name }}SummarizerAgent()

        self.agents = [
            self.retrieval_agent,
            self.enrichment_agent,
            self.summarizer_agent,
        ]

    def execute(self, payload: dict) -> dict:
        """
        Execute the squad workflow in sequence.
        Extend this later for routing, conditional execution, or collaboration.
        """
        result = payload

        for agent in self.agents:
            result = agent.execute(result)

        return result