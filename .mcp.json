{
  "mcpServers": {
    "docling": {
      "type": "http",
      "url": "http://localhost:5001",
      "description": "Docling OCR MCP server — converts PDF, DOCX, and images to structured Markdown. Used by DocumentExtractorAgent in the EOC DocumentIntelligenceSquad. Endpoint: /v1/parse"
    }
  }
}
