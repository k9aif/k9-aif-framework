# Weather Assist (Pure CrewAI)

Standalone CrewAI weather assistant using Open-Meteo.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r examples/weather_assist_crewai/requirements.txt
export OPENAI_API_KEY=your_key_here
export OPENAI_MODEL=gpt-4o-mini