# SPDX-License-Identifier: Apache-2.0
# K9-AIF Weather Assist Lang (LangGraph Integration) — environment template
#
# Copy to .env (gitignored) and fill in for your environment. config.yaml
# references these via ${VAR:-default} expansion — never hardcode a host,
# port, or credential directly in config.yaml.

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b
